import Image from "next/image";
import React from "react";

const GlanceCards = () => {
    return (
        <div className="flex overflow-hidden h-[350px] rounded-2xl bg-white w-full items-center justify-center">
            glance cards
        </div>
    );
};

export default GlanceCards;
