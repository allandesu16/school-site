import React from "react";

const Contacts = () => {
    //staff contacts(accountant,manager, head master, dos, patron, prefet) and adress
    return <div>Contacts</div>;
};

export default Contacts;
